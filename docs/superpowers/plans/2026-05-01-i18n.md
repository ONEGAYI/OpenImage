# i18n 国际化功能实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 为 OpenImage 前端添加中英文国际化支持，引入 react-i18next，新增 LanguageSwitcher 组件。

**Architecture:** react-i18next 初始化时同步加载 JSON 翻译文件，默认跟随系统语言，手动切换后持久化到 localStorage。每个组件通过 `useTranslation()` hook 获取 `t()` 函数替换硬编码文本。新增 LanguageSwitcher 组件嵌入 Topbar 齿轮图标左侧。

**Tech Stack:** react-i18next, i18next, React 18, TypeScript

---

### Task 1: 安装依赖 + 搭建 i18n 基础设施

**Files:**
- Create: `frontend/src/i18n/index.ts`
- Create: `frontend/src/i18n/en.json`
- Create: `frontend/src/i18n/zh.json`
- Modify: `frontend/src/main.tsx`

- [ ] **Step 1: 安装 react-i18next 和 i18next**

```bash
cd frontend && npm install react-i18next i18next
```

- [ ] **Step 2: 创建 `frontend/src/i18n/en.json`**

```json
{
  "common.cancel": "Cancel",
  "common.settings": "Settings",
  "common.generating": "Generating...",

  "app.backendTimeout": "Backend failed to start within 30 seconds",
  "app.backendFailed": "Failed to start backend",
  "app.starting": "Starting OpenImage...",

  "topbar.settings": "Settings",
  "topbar.toggleTheme": "Toggle theme",
  "topbar.language": "Language",

  "sidebar.newSession": "+ New Session",
  "sidebar.searchPlaceholder": "Search sessions...",
  "sidebar.noMatch": "No matching sessions",
  "sidebar.noSessions": "No sessions yet",
  "sidebar.images": "images",
  "sidebar.sessionName": "Session {{count}}",
  "sidebar.rename": "Rename",
  "sidebar.delete": "Delete",

  "gallery.loading": "Loading...",
  "gallery.emptyTitle": "No images yet",
  "gallery.emptySubtitle": "Generate an image to get started",
  "gallery.generating": "Generating...",
  "gallery.step": "Step {{step}}",

  "detail.selectImage": "Select an image",
  "detail.step": "Step",
  "detail.prompt": "Prompt",
  "detail.revisedPrompt": "Revised Prompt",
  "detail.size": "Size",
  "detail.quality": "Quality",
  "detail.format": "Format",
  "detail.created": "Created",
  "detail.selection": "Selection",
  "detail.imagesSelected": "{{count}} images selected",
  "detail.steps": "Steps",
  "detail.prompts": "Prompts",
  "detail.stepLabel": "Step {{step}}:",
  "detail.view": "View",
  "detail.saveImage": "Save Image",
  "detail.saveAll": "Save All",
  "detail.copyPrompt": "Copy Prompt",
  "detail.copyPrompts": "Copy Prompts",
  "detail.forkFromHere": "Fork from Here",
  "detail.forkFromLast": "Fork from Last",
  "detail.inpaint": "Inpaint",
  "detail.remove": "Remove",
  "detail.removing": "Removing...",
  "detail.removeSelected": "Remove Selected",
  "detail.mixed": "Mixed",

  "input.forkingFrom": "Forking from {{id}}...",
  "input.inpaintThisImage": "Inpaint this image",
  "input.attachImage": "Attach image",
  "input.attach": "Attach",
  "input.ctrlEnterToSend": "Ctrl+Enter to send",
  "input.placeholder": "Describe the image you want to generate...",
  "input.noSessionPlaceholder": "Select or create a session first",
  "input.generate": "Generate",
  "input.fromAttachment": "From attachment",

  "settings.title": "Settings",
  "settings.apiKey": "API Key",
  "settings.apiKeyPlaceholder": "sk-...",
  "settings.baseUrl": "API Base URL",
  "settings.baseUrlPlaceholder": "https://api.openai.com/v1",
  "settings.baseUrlHint": "Leave empty to use the OpenAI default address",
  "settings.apiMode": "API Mode",
  "settings.modeChat": "Chat Completions (/v1/chat/completions, recommended)",
  "settings.modeImages": "Images API (/v1/images/generations)",
  "settings.modeResponses": "Responses API (OpenAI native, supports multi-turn editing)",
  "settings.modeHint": "Third-party proxies: Chat Completions or Images API recommended",
  "settings.endpoint": "Endpoint",
  "settings.modelName": "Model Name",
  "settings.modelNamePlaceholder": "gpt-image-2",
  "settings.modelNameHint": "Image generation model ID, e.g. gpt-image-2, gemini-2.5-flash-image",
  "settings.saved": "Settings saved",
  "settings.saving": "Saving...",
  "settings.save": "Save",

  "ratio.tooltip": "Ratio & Size",
  "ratio.ratio": "Ratio",
  "ratio.size": "Size",

  "mask.title": "Inpaint Editor",
  "mask.clear": "Clear",
  "mask.placeholder": "Describe what to generate in the masked area...",
  "mask.sourceGenerated": "Generated image",
  "mask.sourceAttachment": "From attachment",
  "mask.brush": "Brush",
  "mask.rectangle": "Rectangle",
  "mask.eraser": "Eraser",
  "mask.fit": "Fit",

  "error.generateFailed": "Generation failed. Please check your API settings or network connection."
}
```

- [ ] **Step 3: 创建 `frontend/src/i18n/zh.json`**

```json
{
  "common.cancel": "取消",
  "common.settings": "设置",
  "common.generating": "生成中...",

  "app.backendTimeout": "后端未能在 30 秒内启动",
  "app.backendFailed": "后端启动失败",
  "app.starting": "正在启动 OpenImage...",

  "topbar.settings": "设置",
  "topbar.toggleTheme": "切换主题",
  "topbar.language": "语言",

  "sidebar.newSession": "+ 新建会话",
  "sidebar.searchPlaceholder": "搜索会话...",
  "sidebar.noMatch": "无匹配会话",
  "sidebar.noSessions": "暂无会话",
  "sidebar.images": "张图片",
  "sidebar.sessionName": "会话 {{count}}",
  "sidebar.rename": "重命名",
  "sidebar.delete": "删除",

  "gallery.loading": "加载中...",
  "gallery.emptyTitle": "暂无图片",
  "gallery.emptySubtitle": "生成一张图片来开始",
  "gallery.generating": "生成中...",
  "gallery.step": "步骤 {{step}}",

  "detail.selectImage": "选择一张图片",
  "detail.step": "步骤",
  "detail.prompt": "提示词",
  "detail.revisedPrompt": "修订提示词",
  "detail.size": "尺寸",
  "detail.quality": "质量",
  "detail.format": "格式",
  "detail.created": "创建时间",
  "detail.selection": "已选择",
  "detail.imagesSelected": "已选择 {{count}} 张图片",
  "detail.steps": "步骤",
  "detail.prompts": "提示词",
  "detail.stepLabel": "步骤 {{step}}：",
  "detail.view": "查看",
  "detail.saveImage": "保存图片",
  "detail.saveAll": "保存全部",
  "detail.copyPrompt": "复制提示词",
  "detail.copyPrompts": "复制提示词",
  "detail.forkFromHere": "从此处分叉",
  "detail.forkFromLast": "从末尾分叉",
  "detail.inpaint": "局部重绘",
  "detail.remove": "删除",
  "detail.removing": "删除中...",
  "detail.removeSelected": "删除选中",
  "detail.mixed": "混合",

  "input.forkingFrom": "正在从 {{id}} 分叉...",
  "input.inpaintThisImage": "局部重绘此图片",
  "input.attachImage": "附加图片",
  "input.attach": "附加",
  "input.ctrlEnterToSend": "Ctrl+Enter 发送",
  "input.placeholder": "描述你想要生成的图片...",
  "input.noSessionPlaceholder": "请先选择或创建会话",
  "input.generate": "生成",
  "input.fromAttachment": "来自附件",

  "settings.title": "设置",
  "settings.apiKey": "API 密钥",
  "settings.apiKeyPlaceholder": "sk-...",
  "settings.baseUrl": "API 基础 URL",
  "settings.baseUrlPlaceholder": "https://api.openai.com/v1",
  "settings.baseUrlHint": "留空则使用 OpenAI 默认地址",
  "settings.apiMode": "API 模式",
  "settings.modeChat": "Chat Completions（/v1/chat/completions，推荐）",
  "settings.modeImages": "Images API（/v1/images/generations）",
  "settings.modeResponses": "Responses API（OpenAI 原生，支持多轮编辑）",
  "settings.modeHint": "第三方代理推荐 Chat Completions 或 Images API",
  "settings.endpoint": "端点",
  "settings.modelName": "模型名称",
  "settings.modelNamePlaceholder": "gpt-image-2",
  "settings.modelNameHint": "图像生成模型 ID，如 gpt-image-2、gemini-2.5-flash-image 等",
  "settings.saved": "设置已保存",
  "settings.saving": "保存中...",
  "settings.save": "保存",

  "ratio.tooltip": "比例和尺寸",
  "ratio.ratio": "比例",
  "ratio.size": "尺寸",

  "mask.title": "局部重绘编辑器",
  "mask.clear": "清除",
  "mask.placeholder": "描述蒙版区域要生成的内容...",
  "mask.sourceGenerated": "生成图片",
  "mask.sourceAttachment": "来自附件",
  "mask.brush": "笔刷",
  "mask.rectangle": "矩形",
  "mask.eraser": "橡皮擦",
  "mask.fit": "适配",

  "error.generateFailed": "生成失败，请检查 API 设置或网络连接"
}
```

- [ ] **Step 4: 创建 `frontend/src/i18n/index.ts`**

```ts
import i18n from 'i18next';
import { initReactI18next } from 'react-i18next';
import en from './en.json';
import zh from './zh.json';

i18n.use(initReactI18next).init({
  resources: {
    en: { translation: en },
    zh: { translation: zh },
  },
  lng: localStorage.getItem('i18nextLng') ||
    (navigator.language.startsWith('zh') ? 'zh' : 'en'),
  fallbackLng: 'en',
  interpolation: { escapeValue: false },
});

export default i18n;
```

- [ ] **Step 5: 修改 `frontend/src/main.tsx` — 在 React 渲染前 import i18n**

在文件顶部（`import App` 之后）添加一行：

```ts
import "./i18n";
```

完整的 import 区域变为：

```ts
import React from "react";
import ReactDOM from "react-dom/client";
import App from "./App";
import "./i18n";
import "./styles/globals.css";
```

- [ ] **Step 6: 验证构建**

```bash
cd frontend && npm run build
```

预期：构建成功，无 TypeScript 错误。

- [ ] **Step 7: 提交**

```bash
git add frontend/src/i18n/ frontend/src/main.tsx frontend/package.json frontend/package-lock.json
git commit -m "feat: 添加 i18n 基础设施（react-i18next + 中英文翻译文件）

- 安装 react-i18next 和 i18next 依赖
- 创建 i18n/index.ts 初始化配置（系统语言检测 + localStorage 持久化）
- 创建 en.json / zh.json 翻译文件（共 95 个 key）
- 在 main.tsx 中 import i18n 确保渲染前翻译就绪"
```

---

### Task 2: 创建 LanguageSwitcher 组件

**Files:**
- Create: `frontend/src/components/LanguageSwitcher.tsx`

- [ ] **Step 1: 创建 `frontend/src/components/LanguageSwitcher.tsx`**

```tsx
import { useState, useEffect, useRef } from "react";
import { useTranslation } from "react-i18next";

const LANGUAGES = [
  { code: "en", label: "English", badge: "EN" },
  { code: "zh", label: "简体中文", badge: "CN" },
] as const;

export default function LanguageSwitcher() {
  const { i18n } = useTranslation();
  const [open, setOpen] = useState(false);
  const ref = useRef<HTMLDivElement>(null);

  useEffect(() => {
    if (!open) return;
    const handleClick = (e: MouseEvent) => {
      if (ref.current && !ref.current.contains(e.target as Node)) {
        setOpen(false);
      }
    };
    document.addEventListener("mousedown", handleClick);
    return () => document.removeEventListener("mousedown", handleClick);
  }, [open]);

  const currentLang = LANGUAGES.find((l) => l.code === i18n.language) ?? LANGUAGES[0];

  const handleChange = (code: string) => {
    i18n.changeLanguage(code);
    setOpen(false);
  };

  return (
    <div ref={ref} style={{ position: "relative" }}>
      <button
        onClick={() => setOpen(!open)}
        className="flex items-center justify-center rounded-lg transition-colors"
        style={{ width: 34, height: 34, color: "var(--muted)", position: "relative" }}
        onMouseEnter={(e) => {
          e.currentTarget.style.background = "var(--sand)";
          e.currentTarget.style.color = "var(--fg)";
        }}
        onMouseLeave={(e) => {
          e.currentTarget.style.background = "none";
          e.currentTarget.style.color = "var(--muted)";
        }}
        title={i18n.t("topbar.language")}
      >
        <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="1.5" strokeLinecap="round" strokeLinejoin="round">
          <circle cx="12" cy="12" r="10" />
          <ellipse cx="12" cy="12" rx="4" ry="10" />
          <path d="M2 12h20" />
          <path d="M4.5 7h15M4.5 17h15" />
        </svg>
        <span
          style={{
            position: "absolute",
            bottom: 2,
            right: 0,
            fontSize: 7,
            fontWeight: 700,
            color: "var(--accent)",
            background: "var(--bg)",
            padding: "0 1px",
            borderRadius: 2,
            lineHeight: 1.2,
            letterSpacing: "-0.02em",
          }}
        >
          {currentLang.badge}
        </span>
      </button>

      {open && (
        <div
          style={{
            position: "absolute",
            top: "100%",
            right: 0,
            marginTop: 6,
            minWidth: 160,
            background: "var(--surface)",
            border: "1px solid var(--border)",
            borderRadius: "var(--radius-sm)",
            boxShadow: "0 4px 16px rgba(0,0,0,0.08)",
            zIndex: 100,
            padding: 4,
          }}
        >
          {/* 三角箭头 */}
          <div
            style={{
              position: "absolute",
              top: -6,
              right: 11,
              width: 12,
              height: 12,
              background: "var(--surface)",
              borderLeft: "1px solid var(--border)",
              borderTop: "1px solid var(--border)",
              transform: "rotate(45deg)",
            }}
          />
          {LANGUAGES.map((lang) => {
            const active = lang.code === i18n.language;
            return (
              <div
                key={lang.code}
                onClick={() => handleChange(lang.code)}
                className="flex items-center gap-2.5 rounded-md cursor-pointer transition-colors"
                style={{
                  padding: "8px 12px",
                  fontSize: 13,
                  fontWeight: active ? 500 : 400,
                  background: active ? "var(--sand)" : "transparent",
                  color: active ? "var(--fg)" : "var(--muted)",
                }}
                onMouseEnter={(e) => {
                  if (!active) {
                    e.currentTarget.style.background = "var(--sand)";
                    e.currentTarget.style.color = "var(--fg)";
                  }
                }}
                onMouseLeave={(e) => {
                  if (!active) {
                    e.currentTarget.style.background = "transparent";
                    e.currentTarget.style.color = "var(--muted)";
                  }
                }}
              >
                <span
                  style={{
                    fontSize: 14,
                    fontWeight: 600,
                    color: active ? "var(--accent)" : "inherit",
                    minWidth: 28,
                  }}
                >
                  {lang.badge}
                </span>
                <span>{lang.label}</span>
                {active && (
                  <svg
                    style={{ marginLeft: "auto" }}
                    width="16"
                    height="16"
                    viewBox="0 0 24 24"
                    fill="none"
                    stroke="var(--accent)"
                    strokeWidth="2"
                    strokeLinecap="round"
                  >
                    <polyline points="20 6 9 17 4 12" />
                  </svg>
                )}
              </div>
            );
          })}
        </div>
      )}
    </div>
  );
}
```

- [ ] **Step 2: 验证构建**

```bash
cd frontend && npm run build
```

预期：构建成功。

- [ ] **Step 3: 提交**

```bash
git add frontend/src/components/LanguageSwitcher.tsx
git commit -m "feat: 添加 LanguageSwitcher 组件

- 地球 SVG 图标 + 右下角语言码徽章（EN/CN）
- 向下弹出下拉菜单，三角箭头指向按钮
- 当前语言高亮 + 勾选图标
- 点击外部自动关闭"
```

---

### Task 3: 集成 LanguageSwitcher 到 Topbar + 翻译 Topbar

**Files:**
- Modify: `frontend/src/components/Topbar.tsx`

- [ ] **Step 1: 修改 Topbar.tsx**

变更内容：
1. 添加 `import { useTranslation } from "react-i18next"` 和 `import LanguageSwitcher from "./LanguageSwitcher"`
2. 在组件内部添加 `const { t } = useTranslation()`
3. 在齿轮按钮**左侧**插入 `<LanguageSwitcher />`
4. 替换 `title="Settings"` → `title={t("topbar.settings")}`
5. 替换 `title="Toggle theme"` → `title={t("topbar.toggleTheme")}`

Topbar.tsx 完整替换后内容：

```tsx
import { useSessionStore } from "../stores/sessionStore";
import { useTheme } from "../hooks/useTheme";
import { useTranslation } from "react-i18next";
import LanguageSwitcher from "./LanguageSwitcher";

interface TopbarProps {
  onOpenSettings: () => void;
}

export default function Topbar({ onOpenSettings }: TopbarProps) {
  const { sessions, activeSessionId } = useSessionStore();
  const { theme, toggleTheme } = useTheme();
  const { t } = useTranslation();

  const activeSession = sessions.find((s) => s.id === activeSessionId);
  const title = activeSession?.name ?? "OpenImage";

  return (
    <header
      className="flex items-center justify-between px-5 border-b"
      style={{
        height: "var(--topbar-h)",
        minHeight: "var(--topbar-h)",
        borderColor: "var(--border-s)",
        background: "var(--bg)",
        transition: "background 0.3s, border-color 0.3s",
      }}
    >
      <div
        className="font-semibold"
        style={{
          fontFamily: "var(--font-display)",
          fontSize: "16px",
          color: "var(--fg)",
          letterSpacing: "-0.01em",
        }}
      >
        {title}
      </div>

      <div className="flex items-center gap-1.5">
        <LanguageSwitcher />

        {/* Settings button */}
        <button
          onClick={onOpenSettings}
          className="flex items-center justify-center rounded-lg transition-colors"
          style={{
            width: 34,
            height: 34,
            color: "var(--muted)",
          }}
          onMouseEnter={(e) => {
            e.currentTarget.style.background = "var(--sand)";
            e.currentTarget.style.color = "var(--fg)";
          }}
          onMouseLeave={(e) => {
            e.currentTarget.style.background = "none";
            e.currentTarget.style.color = "var(--muted)";
          }}
          title={t("topbar.settings")}
        >
          <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="1.5" strokeLinecap="round">
            <path d="M12.22 2h-.44a2 2 0 00-2 2v.18a2 2 0 01-1 1.73l-.43.25a2 2 0 01-2 0l-.15-.08a2 2 0 00-2.73.73l-.22.38a2 2 0 00.73 2.73l.15.1a2 2 0 011 1.72v.51a2 2 0 01-1 1.74l-.15.09a2 2 0 00-.73 2.73l.22.38a2 2 0 002.73.73l.15-.08a2 2 0 012 0l.43.25a2 2 0 011 1.73V20a2 2 0 002 2h.44a2 2 0 002-2v-.18a2 2 0 011-1.73l.43-.25a2 2 0 012 0l.15.08a2 2 0 002.73-.73l.22-.39a2 2 0 00-.73-2.73l-.15-.08a2 2 0 01-1-1.74v-.5a2 2 0 011-1.74l.15-.09a2 2 0 00.73-2.73l-.22-.38a2 2 0 00-2.73-.73l-.15.08a2 2 0 01-2 0l-.43-.25a2 2 0 01-1-1.73V4a2 2 0 00-2-2z" />
            <circle cx="12" cy="12" r="3" />
          </svg>
        </button>

        {/* Theme toggle */}
        <button
          onClick={toggleTheme}
          className="relative rounded-[14px] cursor-pointer border overflow-hidden"
          style={{
            width: 50,
            height: 28,
            background: "var(--sand)",
            borderColor: "var(--border)",
            transition: "background 0.3s",
            padding: 0,
          }}
          title={t("topbar.toggleTheme")}
        >
          <span
            className="absolute inset-0 flex items-center justify-between px-1.5 pointer-events-none"
            style={{ fontSize: 13 }}
          >
            <span>&#9728;</span>
            <span>&#9790;</span>
          </span>
          <span
            className="absolute top-[3px] left-[3px] w-5 h-5 rounded-full"
            style={{
              background: "var(--accent)",
              transform: theme === "dark" ? "translateX(22px)" : "translateX(0)",
              transition: "transform 0.3s cubic-bezier(0.4,0,0.2,1)",
              boxShadow: "0 1px 3px rgba(0,0,0,0.15)",
            }}
          />
        </button>
      </div>
    </header>
  );
}
```

- [ ] **Step 2: 提交**

```bash
git add frontend/src/components/Topbar.tsx
git commit -m "feat: Topbar 集成 LanguageSwitcher 并翻译 tooltip

- 在齿轮按钮左侧插入 LanguageSwitcher 组件
- 替换 Settings 和 Toggle theme 的 title 属性为 t() 调用"
```

---

### Task 4: 翻译 App.tsx + Sidebar.tsx

**Files:**
- Modify: `frontend/src/App.tsx`
- Modify: `frontend/src/components/Sidebar.tsx`

- [ ] **Step 1: 修改 App.tsx**

在文件顶部添加 import：

```ts
import { useTranslation } from "react-i18next";
```

在 `App()` 函数体开头添加：

```ts
const { t } = useTranslation();
```

替换 3 个硬编码字符串：

- 第 18 行 `"Backend failed to start within 30 seconds"` → `t("app.backendTimeout")`
- 第 68 行 `Failed to start backend` → `{t("app.backendFailed")}`
- 第 94 行 `Starting OpenImage...` → `{t("app.starting")}`

- [ ] **Step 2: 修改 Sidebar.tsx**

在文件顶部添加 import：

```ts
import { useTranslation } from "react-i18next";
```

在 `Sidebar()` 函数体（`useSessionStore` 解构之后）添加：

```ts
const { t } = useTranslation();
```

替换硬编码字符串：

- 第 45 行 `` `Session ${sessions.length + 1}` `` → `` t("sidebar.sessionName", { count: sessions.length + 1 }) ``
- 第 94 行 `>+ New Session</button>` → `>{t("sidebar.newSession")}</button>`
- 第 100 行 `placeholder="Search sessions..."` → `placeholder={t("sidebar.searchPlaceholder")}`
- 第 112 行 `{searchQuery ? "无匹配会话" : "暂无会话"}` → `{searchQuery ? t("sidebar.noMatch") : t("sidebar.noSessions")}`
- 第 155 行 `{session.image_count ?? 0} images` → `` {session.image_count ?? 0} {t("sidebar.images")} ``
- 第 172 行 `>Rename</button>` → `>{t("sidebar.rename")}</button>`
- 第 176 行 `>Delete</button>` → `>{t("sidebar.delete")}</button>`

- [ ] **Step 3: 验证构建**

```bash
cd frontend && npm run build
```

- [ ] **Step 4: 提交**

```bash
git add frontend/src/App.tsx frontend/src/components/Sidebar.tsx
git commit -m "feat: 翻译 App.tsx 和 Sidebar.tsx 用户可见文本

- App: 后端启动超时/失败/loading 消息
- Sidebar: 新建会话、搜索、空状态、上下文菜单、图片计数"
```

---

### Task 5: 翻译 Gallery.tsx

**Files:**
- Modify: `frontend/src/components/Gallery.tsx`

- [ ] **Step 1: 修改 Gallery.tsx**

添加 import：

```ts
import { useTranslation } from "react-i18next";
```

在 `Gallery()` 函数体开头添加：

```ts
const { t } = useTranslation();
```

替换硬编码字符串：

- 第 10 行 `Loading...` → `{t("gallery.loading")}`
- 第 21 行 `No images yet` → `{t("gallery.emptyTitle")}`
- 第 22 行 `Generate an image to get started` → `{t("gallery.emptySubtitle")}`
- 第 58 行 `` alt={`Step ${img.step}`} `` → `` alt={t("gallery.step", { step: img.step })} ``
- 第 74 行 `Step {img.step}` → `` {t("gallery.step", { step: img.step })} ``

对 "Generating..." 出现的 3 处（第 84、86、92 行）：
- `alt="Generating..."` → `` alt={t("common.generating")} ``
- `<span ...>Generating...</span>` → `<span ...>{t("common.generating")}</span>`
- 第 92 行 `<div className="text-xs" ...>Generating...</div>` → `<div className="text-xs" ...>{t("common.generating")}</div>`

- [ ] **Step 2: 提交**

```bash
git add frontend/src/components/Gallery.tsx
git commit -m "feat: 翻译 Gallery.tsx 用户可见文本

- loading、空状态标题/副标题、步骤标签、生成中状态"
```

---

### Task 6: 翻译 DetailPanel.tsx

**Files:**
- Modify: `frontend/src/components/DetailPanel.tsx`

- [ ] **Step 1: 修改 DetailPanel.tsx**

添加 import：

```ts
import { useTranslation } from "react-i18next";
```

在 `DetailPanel()` 函数体开头添加：

```ts
const { t } = useTranslation();
```

替换所有硬编码字符串（按行号顺序）：

- 第 81 行 `Select an image` → `{t("detail.selectImage")}`
- 第 94 行 `` alt={`Step ${singleImage!.step}`} `` → `` alt={t("gallery.step", { step: singleImage!.step })} ``
- 第 104 行 `<div style={labelStyle}>Step</div>` → `<div style={labelStyle}>{t("detail.step")}</div>`
- 第 105 行 `<div style={labelStyle}>Prompt</div>` → `<div style={labelStyle}>{t("detail.prompt")}</div>`
- 第 106 行 `<div style={labelStyle}>Revised Prompt</div>` → `<div style={labelStyle}>{t("detail.revisedPrompt")}</div>`
- 第 108 行 `"Size"` → `t("detail.size")`, `"Quality"` → `t("detail.quality")`, `"Format"` → `t("detail.format")`

  替换整行为：
  ```tsx
  {[{ label: t("detail.size"), value: singleImage!.size }, { label: t("detail.quality"), value: singleImage!.quality }, { label: t("detail.format"), value: singleImage!.output_format }].map(({ label, value }) => (
  ```

- 第 112 行 `<div style={labelStyle}>Created</div>` → `<div style={labelStyle}>{t("detail.created")}</div>`
- 第 117 行 `<div style={labelStyle}>Selection</div>` → `<div style={labelStyle}>{t("detail.selection")}</div>`
- 第 118 行 `` {selectedImages.length} images selected `` → `` {t("detail.imagesSelected", { count: selectedImages.length })} ``
- 第 121 行 `<div style={labelStyle}>Steps</div>` → `<div style={labelStyle}>{t("detail.steps")}</div>`
- 第 122 行 `<div style={labelStyle}>Format</div>` → `<div style={labelStyle}>{t("detail.format")}</div>`
- 第 124 行 `<div style={labelStyle}>Prompts</div>` → `<div style={labelStyle}>{t("detail.prompts")}</div>`
- 第 127 行 `` Step {img.step}: `` → `` {t("detail.stepLabel", { step: img.step })} ``

按钮区域（第 155-218 行）：
- `>View</button>` → `>{t("detail.view")}</button>`
- `>Save Image</button>` → `>{t("detail.saveImage")}</button>`
- `` {deleting ? "Removing..." : "Remove"} `` → `` {deleting ? t("detail.removing") : t("detail.remove")} ``
- `` {deleting ? "Removing..." : "Remove Selected"} `` → `` {deleting ? t("detail.removing") : t("detail.removeSelected")} ``
- `>Save All</button>` → `>{t("detail.saveAll")}</button>`
- `>Copy Prompt</button>` → `>{t("detail.copyPrompt")}</button>`
- `>Fork from Here</button>` → `>{t("detail.forkFromHere")}</button>`
- `>Inpaint</button>` → `>{t("detail.inpaint")}</button>`
- `>Copy Prompts</button>` → `>{t("detail.copyPrompts")}</button>`
- `>Fork from Last</button>` → `>{t("detail.forkFromLast")}</button>`

底部 MaskEditor 的错误回退（第 261 行）：
- `"生成失败，请检查 API 设置或网络连接"` → `t("error.generateFailed")`

全屏查看器（第 283 行）：
- `` alt={`Step ${viewingImage.step}`} `` → `` alt={t("gallery.step", { step: viewingImage.step })} ``

MultiPreview 子组件（第 348 行）：
- `` alt={`Step ${img.step}`} `` → `` alt={t("gallery.step", { step: img.step })} ``

  注意：MultiPreview 不是独立组件 export，在 DetailPanel 内部，可以直接使用 `t`（通过闭包或 prop）。但因为它定义在 `DetailPanel` 函数外部，需要将 `t` 作为 prop 传入，或者在该子组件中也调用 `useTranslation()`。推荐后者：

  ```tsx
  function MultiPreview({ images }: { images: Image[] }) {
    const { t } = useTranslation();
    // ... 其余不变，只替换 alt 文本
  ```

getCommonValue 函数（第 385 行）：
- `"Mixed"` → 由于这是纯函数不在组件内，改为接受翻译函数作为参数：

  ```tsx
  function getCommonValue(images: Image[], field: keyof Image): string {
    const values = new Set(images.map((img) => img[field]));
    return values.size === 1 ? String([...values][0]) : "Mixed";
  }
  ```

  对于 "Mixed" 的翻译，由于 `getCommonValue` 是纯函数在组件外，最简方案是直接在 DetailPanel 组件内的调用处判断：

  第 122 行：
  ```tsx
  <div className="flex-1"><div style={labelStyle}>{t("detail.format")}</div><div className="text-[13px]" style={{ color: "var(--fg)" }}>{(() => { const v = getCommonValue(selectedImages, "output_format"); return v === "Mixed" ? t("detail.mixed") : v; })()}</div></div>
  ```

- [ ] **Step 2: 验证构建**

```bash
cd frontend && npm run build
```

- [ ] **Step 3: 提交**

```bash
git add frontend/src/components/DetailPanel.tsx
git commit -m "feat: 翻译 DetailPanel.tsx 用户可见文本

- 25+ 个标签、按钮、状态文本替换为 t() 调用
- MaskEditor 错误回退、MultiPreview alt 文本同步翻译"
```

---

### Task 7: 翻译 InputArea.tsx + RatioSelector.tsx

**Files:**
- Modify: `frontend/src/components/InputArea.tsx`
- Modify: `frontend/src/components/RatioSelector.tsx`

- [ ] **Step 1: 修改 InputArea.tsx**

添加 import：

```ts
import { useTranslation } from "react-i18next";
```

在 `InputArea()` 函数体开头添加：

```ts
const { t } = useTranslation();
```

替换硬编码字符串：

- 第 113 行 `<span>Forking from {pendingForkFrom.slice(0, 16)}...</span>` → `` <span>{t("input.forkingFrom", { id: pendingForkFrom.slice(0, 16) })}</span> ``
- 第 117 行 `>Cancel</button>` → `>{t("common.cancel")}</button>`
- 第 133 行 `title="Inpaint this image"` → `title={t("input.inpaintThisImage")}`
- 第 158 行 `title="Attach image"` → `title={t("input.attachImage")}`
- 第 161 行 `Attach` → `{t("input.attach")}`
- 第 170 行 `title="Settings"` → `title={t("common.settings")}`
- 第 173 行 `Settings` → `{t("common.settings")}`
- 第 178 行 `Ctrl+Enter to send` → `{t("input.ctrlEnterToSend")}`
- 第 189 行 `placeholder={activeSessionId ? "Describe the image you want to generate..." : "Select or create a session first"}` → `placeholder={activeSessionId ? t("input.placeholder") : t("input.noSessionPlaceholder")}`
- 第 209 行 `>Cancel</button>` → `>{t("common.cancel")}</button>`
- 第 228 行 `>Generate</button>` → `>{t("input.generate")}</button>`
- 第 250 行 `"生成失败，请检查 API 设置或网络连接"` → `t("error.generateFailed")`

- [ ] **Step 2: 修改 RatioSelector.tsx**

添加 import：

```ts
import { useTranslation } from "react-i18next";
```

在 `RatioSelector()` 函数体（`const { ... } = useGenerationStore();` 之后）添加：

```ts
const { t } = useTranslation();
```

替换硬编码字符串：

- 第 120 行 `title="比例和尺寸"` → `title={t("ratio.tooltip")}`
- 第 163 行 `<div style={sectionLabelStyle}>比例</div>` → `<div style={sectionLabelStyle}>{t("ratio.ratio")}</div>`
- 第 196 行 `<div style={sectionLabelStyle}>尺寸</div>` → `<div style={sectionLabelStyle}>{t("ratio.size")}</div>`

- [ ] **Step 3: 验证构建**

```bash
cd frontend && npm run build
```

- [ ] **Step 4: 提交**

```bash
git add frontend/src/components/InputArea.tsx frontend/src/components/RatioSelector.tsx
git commit -m "feat: 翻译 InputArea 和 RatioSelector 用户可见文本

- InputArea: placeholder、按钮、tooltip、分叉状态、错误回退
- RatioSelector: tooltip、比例/尺寸分区标题"
```

---

### Task 8: 翻译 SettingsDialog.tsx

**Files:**
- Modify: `frontend/src/components/SettingsDialog.tsx`

- [ ] **Step 1: 修改 SettingsDialog.tsx**

添加 import：

```ts
import { useTranslation } from "react-i18next";
```

在 `SettingsDialog()` 函数体开头添加：

```ts
const { t } = useTranslation();
```

替换硬编码字符串：

- 第 68 行 `Settings` (标题) → `{t("settings.title")}`
- 第 71 行 `API Key` (label) → `{t("settings.apiKey")}`
- 第 74 行 `placeholder="sk-..."` → `placeholder={t("settings.apiKeyPlaceholder")}`
- 第 81 行 `API Base URL` (label) → `{t("settings.baseUrl")}`
- 第 84 行 `placeholder="https://api.openai.com/v1"` → `placeholder={t("settings.baseUrlPlaceholder")}`
- 第 90 行 `留空则使用 OpenAI 默认地址` → `{t("settings.baseUrlHint")}`
- 第 92 行 `API 模式` (label) → `{t("settings.apiMode")}`
- 第 98 行 `<option value="chat">Chat Completions（/v1/chat/completions，推荐）</option>` → `<option value="chat">{t("settings.modeChat")}</option>`
- 第 99 行 `<option value="images">Images API（/v1/images/generations）</option>` → `<option value="images">{t("settings.modeImages")}</option>`
- 第 100 行 `<option value="responses">Responses API（OpenAI 原生，支持多轮编辑）</option>` → `<option value="responses">{t("settings.modeResponses")}</option>`
- 第 102 行 `第三方代理推荐 Chat Completions 或 Images API` → `{t("settings.modeHint")}`
- 第 106 行 `端点 ` → `{t("settings.endpoint")} `
- 第 110 行 `模型名称` (label) → `{t("settings.modelName")}`
- 第 113 行 `placeholder="gpt-image-2"` → `placeholder={t("settings.modelNamePlaceholder")}`
- 第 119 行 `图像生成模型 ID，如 gpt-image-2、gemini-2.5-flash-image 等` → `{t("settings.modelNameHint")}`
- 第 35 行 `"Settings saved"` → `t("settings.saved")`
- 第 133 行 `Cancel` → `{t("common.cancel")}`
- 第 142 行 `` {saving ? "Saving..." : "Save"} `` → `` {saving ? t("settings.saving") : t("settings.save")} ``

- [ ] **Step 2: 验证构建**

```bash
cd frontend && npm run build
```

- [ ] **Step 3: 提交**

```bash
git add frontend/src/components/SettingsDialog.tsx
git commit -m "feat: 翻译 SettingsDialog.tsx 用户可见文本

- 所有表单标签、提示文字、按钮、选项文本替换为 t() 调用"
```

---

### Task 9: 翻译 MaskEditor 组件

**Files:**
- Modify: `frontend/src/components/MaskEditor/index.tsx`
- Modify: `frontend/src/components/MaskEditor/ToolBar.tsx`

- [ ] **Step 1: 修改 MaskEditor/index.tsx**

添加 import：

```ts
import { useTranslation } from "react-i18next";
```

在 `MaskEditor()` 函数体开头添加：

```ts
const { t } = useTranslation();
```

替换硬编码字符串：

- 第 53-56 行 `sourceLabel` 赋值 — 保持原有逻辑（generated 显示 imageId），只翻译 "来自附件"：
  ```tsx
  const sourceLabel =
    source.type === "generated"
      ? source.imageId
      : t("mask.sourceAttachment");
  ```

- 第 88 行 `Inpaint Editor` → `{t("mask.title")}`
- 第 105 行 `Cancel` → `{t("common.cancel")}`
- 第 119 行 `Clear` → `{t("mask.clear")}`
- 第 170 行 `placeholder="Describe what to generate in the masked area..."` → `placeholder={t("mask.placeholder")}`
- 第 202 行 `` {generating ? "Generating..." : "Generate"} `` → `` {generating ? t("common.generating") : t("input.generate")} ``

- [ ] **Step 2: 修改 MaskEditor/ToolBar.tsx**

添加 import：

```ts
import { useTranslation } from "react-i18next";
```

在 `ToolBar()` 函数体开头添加：

```ts
const { t } = useTranslation();
```

修改 TOOLS 常量 — 改为在组件内部使用翻译（因为 TOOLS 是模块级常量，`t` 函数在组件外部不可用）。将 TOOLS 中的 label 改为 key，在渲染时翻译：

```tsx
const TOOLS: { id: Tool; labelKey: string; icon: string }[] = [
  { id: "brush", labelKey: "mask.brush", icon: "M12 19l7-7 3 3-7 7-3-3z M18 13l-1.5-7.5L2 2l3.5 14.5L13 18l5-5z" },
  { id: "rectangle", labelKey: "mask.rectangle", icon: "M3 3h18v18H3z" },
  { id: "eraser", labelKey: "mask.eraser", icon: "M20 20H7L3 16l9-9 8 8-4 4z" },
];
```

然后在渲染处替换 `title={label}` → `title={t(item.labelKey)}`：

```tsx
{TOOLS.map(({ id, labelKey, icon }) => (
  <button
    key={id}
    onClick={() => onToolChange(id)}
    title={t(labelKey)}
    // ... 其余样式不变
  >
```

同时翻译底部的 "Fit" 按钮（第 118 行）：
- `Fit` → `{t("mask.fit")}`

- [ ] **Step 3: 验证构建**

```bash
cd frontend && npm run build
```

- [ ] **Step 4: 提交**

```bash
git add frontend/src/components/MaskEditor/index.tsx frontend/src/components/MaskEditor/ToolBar.tsx
git commit -m "feat: 翻译 MaskEditor 组件用户可见文本

- 编辑器标题、按钮、placeholder、工具名称
- ToolBar 工具 tooltip 和缩放适配按钮"
```

---

### Task 10: 构建验证 + 最终清理

**Files:**
- 无新文件

- [ ] **Step 1: 完整生产构建**

```bash
cd frontend && npm run build
```

预期：构建成功，无 TypeScript 错误、无 missing key 警告。

- [ ] **Step 2: 开发模式启动验证**

```bash
cd frontend && npm run dev
```

在浏览器中打开 http://localhost:1420 ，验证：

1. 语言切换器出现在顶栏齿轮图标左侧
2. 点击展开下拉菜单，显示 EN 和 CN 选项
3. 切换到中文 — 所有界面文字应变为中文
4. 切换回英文 — 所有界面文字恢复英文
5. 刷新页面 — 语言选择应持久化（localStorage）
6. 浅色/深色主题切换正常，下拉菜单配色跟随主题

- [ ] **Step 3: 最终提交（如有遗漏修复）**

```bash
git add -A
git commit -m "fix: i18n 集成后的遗漏修复"
```
